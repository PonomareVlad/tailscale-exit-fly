app = 'fly-tailscale-exit'
primary_region = 'fra'

[[restart]]
  policy = "always"

[[vm]]
  cpus = 1
  memory_mb = 256
  memory = '256mb'
  cpu_kind = 'shared'
  size = 'shared-cpu-1x'
  persist_rootfs = "always"

[deploy]
  strategy = "immediate"
